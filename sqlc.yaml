version: "2"
sql:
  - engine: "sqlite"
    queries: "db/sql/queries.sql"
    schema: "db/sql/schema.sql"
    database:
      uri: "file:database.db"
    gen:
      go:
        package: "db"
        out: "db"

